# Re:Check v0.1 完了チェックリスト

## 使い方
- 各項目を `PASS / FAIL / PARTIAL` で確認する
- 必要なら備考欄にメモを書く
- v0.1 完了判定は、**必須項目がすべて PASS** を目安にする

---

## 0. 基本情報
- 確認日:
- 確認者:
- 対象バージョン / コミット:
- OS:
- Python:
- 備考:

---

## 1. プロジェクト管理

### 1-1. 新規プロジェクト作成
- [ ] 新規プロジェクトを作成できる
- [ ] 作成直後、Base が空で始まる
- [ ] 作成直後、Compare が空で始まる
- [ ] 作成直後、Diff Results が空になる
- [ ] 作成直後、Preview が空になる
- [ ] 作成直後、「まだスナップショット未保存」の案内が出る
- [ ] 前プロジェクトの状態が残らない

備考:

### 1-2. プロジェクト切替
- [ ] 保存済みプロジェクトを切り替えられる
- [ ] 切替後、そのプロジェクトの snapshots / logs のみ読み込まれる
- [ ] 前プロジェクトの Base / Compare が残らない
- [ ] 切替後の Diff / Preview が正しく更新される

備考:

### 1-3. プロジェクト設定
- [ ] プロジェクト名を設定できる
- [ ] ルートフォルダを設定できる
- [ ] スナップショット保存先を設定できる
- [ ] 除外ルールを設定できる
- [ ] プロジェクトメニュー (`...`) が機能する

備考:

---

## 2. スナップショット / 比較

### 2-1. スナップショット保存
- [ ] `Save Snapshot` が実行できる
- [ ] 保存後、新しいスナップショットが作成される
- [ ] 保存後、新スナップショットが Compare に入る
- [ ] スナップショット保存後、Scope ツリーが必要に応じて更新される

備考:

### 2-2. 比較実行
- [ ] Base / Compare を選んで比較できる
- [ ] Base と Compare が同じ場合、ガードが出る
- [ ] スナップショット不足時、比較を止めて案内が出る
- [ ] 未保存状態で Compare を押すと確認ダイアログが出る
- [ ] `Save and Compare` が正しく動く
- [ ] `Compare Without Saving` が正しく動く
- [ ] `Cancel` が何も変えずに終了する

備考:

### 2-3. 比較の意味づけ
- [ ] `比較元 (Base Snapshot)` の意味が画面でわかる
- [ ] `比較先 (Compare Snapshot)` の意味が画面でわかる
- [ ] `前回 → 今回` の見せ方が自然

備考:

---

## 3. Scope

### 3-1. Scope モード
- [ ] Scope モードが `全体 / 選択` の2種だけになっている
- [ ] `全体` でプロジェクト全体の結果が出る
- [ ] `選択` でチェックしたフォルダ配下だけの結果が出る
- [ ] `選択` モードで複数フォルダを同時に扱える
- [ ] `選択` で未チェック時、比較を止めて案内が出る

備考:

### 3-2. Scope ツリー
- [ ] フォルダツリーが表示される
- [ ] 展開 / 折りたたみができる
- [ ] チェック状態が反映される
- [ ] 新規追加フォルダが必要なタイミングで反映される
- [ ] ツリーの差分ヒントがうるさすぎない
- [ ] 初期幅が狭すぎず、中央を圧迫しない

備考:

---

## 4. Diff Results

### 4-1. 一覧表示
- [ ] 結果一覧が表示される
- [ ] 種別 / ファイル名 / 相対パスが主役として見える
- [ ] 1行表示で十分な件数が見える
- [ ] 列ヘッダが読みやすい
- [ ] Base / Compare 更新日時ヘッダが見やすい
- [ ] Base / Compare サイズヘッダが見やすい

備考:

### 4-2. 相対パス表示
- [ ] ファイル名列はファイル名のみになっている
- [ ] 相対パス列は親フォルダパス中心で読める
- [ ] ルート直下ファイルは `（ルート）` と表示される
- [ ] ツールチップ等で必要なら詳細パスが見られる

備考:

### 4-3. サマリー / フィルタ
- [ ] 追加 / 削除 / 更新 / 同一 のサマリーが見える
- [ ] サマリー押下でフィルタできる
- [ ] 色分けが控えめで見やすい
- [ ] 種別フィルタが動く
- [ ] 検索が動く

備考:

### 4-4. 日時表示
- [ ] 画面表示の日時が `YYYY-MM-DD HH:MM:SS` になっている
- [ ] `T` が出ない
- [ ] 小数秒が出ない
- [ ] JST 表示が実用上自然
- [ ] Base / Compare 両方で表記が揃っている

備考:

---

## 5. Preview

### 5-1. 基本
- [ ] 差分行を選ぶと Preview が更新される
- [ ] Base / Compare を左右で見比べられる
- [ ] 追加時、片側が `なし` になる
- [ ] 削除時、片側が `なし` になる
- [ ] 更新時、両側が見られる

備考:

### 5-2. 対応形式
- [ ] 画像プレビューが動く
- [ ] テキストプレビューが動く
- [ ] PDFプレビューが動く
- [ ] 音声プレビューが動く
- [ ] Officeファイルは外部起動できる
- [ ] unsupported は本当に非対応のときだけ出る

備考:

### 5-3. 音声
- [ ] 再生 / 一時停止できる
- [ ] シークできる
- [ ] 長さが表示される
- [ ] 波形が表示される
- [ ] 左右比較が実用になる

備考:

### 5-4. 動画（簡易対応の場合のみ）
- [ ] 動画プレビューまたはサムネイルが機能する
- [ ] 再生できる、または外部起動へ自然に逃がせる

備考:

### 5-5. Preview 開閉
- [ ] 右ペイン内で折りたたみできる
- [ ] 折りたたみ状態が分かりやすい
- [ ] 再度開きやすい
- [ ] 開閉しても他表示が崩れない

備考:

---

## 6. CSV / ログ / 履歴

### 6-1. CSV 出力
- [ ] Compare 実行時に CSV が自動保存される
- [ ] ファイル名に日時が入っている
- [ ] 保存先がわかる
- [ ] `Open Latest Compare CSV` が動く
- [ ] `Open Compare CSV Folder` が動く

備考:

### 6-2. CSV 内容
- [ ] `kind` が入っている
- [ ] `filename` が入っている
- [ ] `relative_path` が入っている
- [ ] `base_modified` が入っている
- [ ] `compare_modified` が入っている
- [ ] `base_size` が入っている
- [ ] `compare_size` が入っている
- [ ] JST として実用的な日時になっている

備考:

### 6-3. 履歴
- [ ] History ボタンから履歴を開ける
- [ ] スナップショット一覧が見える
- [ ] Saved Compares が見える
- [ ] Base に設定できる
- [ ] Compare に設定できる
- [ ] 結果を開ける

備考:

---

## 7. レイアウト / 操作性

### 7-1. 初期レイアウト
- [ ] Scope が狭すぎず広すぎない
- [ ] Diff Results が主役として十分広い
- [ ] Preview が使える幅を持つ
- [ ] 全体として窮屈すぎない

備考:

### 7-2. レイアウト操作
- [ ] スプリッタで幅調整できる
- [ ] 必要ならレイアウト初期化が動く
- [ ] レイアウトを崩しても復帰できる

備考:

### 7-3. 文字サイズ
- [ ] 小 / 中 / 大 が切り替わる
- [ ] 中が標準として自然
- [ ] 一覧の密度と可読性のバランスが悪くならない

備考:

### 7-4. ショートカット
- [ ] `Ctrl+Enter` Compare
- [ ] `Ctrl+S` Save Snapshot
- [ ] `Ctrl+H` History
- [ ] `Ctrl+F` 検索
- [ ] `Esc` Preview を閉じる
- [ ] `Ctrl+0` 文字サイズ標準
- [ ] `Ctrl+=` 文字大
- [ ] `Ctrl+-` 文字小

備考:

---

## 8. README / 配布準備

### 8-1. README
- [ ] ツール概要が書かれている
- [ ] 起動方法が書かれている
- [ ] 基本操作が書かれている
- [ ] CSV 保存先が書かれている
- [ ] 対応プレビュー形式が書かれている
- [ ] 既知の制限が書かれている
- [ ] v0.1 でやらないことが書かれている

備考:

### 8-2. 配布前確認
- [ ] 機密データ / 実案件データが入っていない
- [ ] `.gitignore` が適切
- [ ] 不要ファイルが混ざっていない
- [ ] 他の人に見てほしい観点が整理されている

備考:

---

## 9. 既知の制限（確認用）
- [ ] 動画プレビューが簡易対応または未対応でも把握済み
- [ ] PDF が環境依存 fallback を持つと把握済み
- [ ] naive timestamp の扱いを把握済み
- [ ] move/rename detection は未対応と把握済み

備考:

---

## 10. v0.1 完了判定
- [ ] 必須項目がすべて PASS
- [ ] README が最低限整っている
- [ ] 他人に触ってもらえる状態になっている

最終判定:
- [ ] v0.1 完了
- [ ] v0.1 条件付き完了
- [ ] 継続修正が必要

総評: